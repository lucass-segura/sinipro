# sinipro

## Notas de desarrollo

### Módulos ocultos temporalmente

La sección **"Módulos"** del sidebar (Siniestros, Archivos, Mensajes) está comentada en [`components/app-sidebar.tsx`](components/app-sidebar.tsx).

Para volver a mostrarla, descomentar el bloque marcado con `{/* MÓDULOS PRÓXIMAMENTE */}` en ese archivo.